{
  "name": "okx-agentic-contest-trader-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "validate:config": "node --experimental-strip-types scripts/validate_config.ts",
    "check:compliance": "node --experimental-strip-types scripts/check_compliance.ts",
    "summarize": "node --experimental-strip-types scripts/summarize_trades.ts"
  }
}
